namespace Domain.Enums;

/// <summary>Vendor notification buckets (matches dashboard tabs).</summary>
public enum NotificationCategory
{
    Order = 1,
    Payment = 2,
    Message = 3,
    Promotion = 4
}
