namespace Domain.Enums;

/// <summary>Engagement events for vendor analytics (stored per vendor).</summary>
public enum VendorTrafficEventType
{
    ProductView = 1,
    ClickThrough = 2,
    Favourite = 3,
    Search = 4
}
